{
  "name": "stack-atlas-backend",
  "version": "1.0.0",
  "description": "Stack Atlas backend — Lambda API handler",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node24 --format=cjs --outdir=dist"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.577.0",
    "aws-jwt-verify": "^5.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  },
  "engines": {
    "node": "24.12.0"
  }
}
