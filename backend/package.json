{
  "name": "stack-atlas-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/handler.js",
  "scripts": {
    "build": "esbuild src/handler.js --bundle --platform=node --target=node24 --format=cjs --outdir=dist"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.577.0",
    "aws-jwt-verify": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  },
  "engines": {
    "node": "24.12.0"
  }
}
